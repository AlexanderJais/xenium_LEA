# xenium_LEA — dataset audit & batch-effect diagnostics
# Install with: pip install -r requirements.txt
# Or, to get the `xenium-lea` command: pip install -e ".[dev]"

numpy >= 1.26
pandas >= 2.1
scipy >= 1.12
scikit-learn >= 1.3
matplotlib >= 3.8
pyarrow >= 14.0        # cells.parquet

pytest >= 8.0          # tests only
